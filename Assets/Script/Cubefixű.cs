using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// new script
/// button give the pointer list to the objects thsat they opened for destroying only them
/// re saving the showroom, maybe checking the saving itself
/// might be related to cube
/// cube
/// coroutine
/// getting the info 
/// put into shape
/// wait second
/// do the placing
/// </summary>
public class Cubefixű : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
